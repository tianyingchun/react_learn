/**
 * @jsx React.DOM
 */

var React = require('react');

var Home = React.createClass({
    render: function() {

        return (
            <div>The home page</div>
        );
    }
});

module.exports = Home;
